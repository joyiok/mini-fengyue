# Nothing in this repository builds an image (Compose pulls prebuilt images), so
# this file only documents what must never be shipped in an image layer if you
# ever add a build context.
.git
.env
.env.*
backups
data
caddy
config/config.yaml
